query Media($id: Int!) {
  Media(id: $id) {
    id
    type
    idMal
    synonyms
    bannerImage
    isFavourite
    description(asHtml: true)
    coverImage {
      color
      extraLarge
    }
    title {
      native
      romaji
      english
      userPreferred
    }
  }
}
