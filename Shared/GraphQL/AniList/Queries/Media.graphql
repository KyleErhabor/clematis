query Media($id: Int!) {
  Media(id: $id) {
    id
    type
    idMal
    format
    bannerImage
    title {
      english
      romaji
      native
      userPreferred
    }
    coverImage {
      color
      extraLarge
    }
  }
}
