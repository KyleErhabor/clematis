query User($id: Int!) {
  User(id: $id) {
    name
    isBlocked
    isFollower
    bannerImage
    isFollowing
    about(asHtml: true)
    avatar {
      large
    }
    options {
      profileColor
    }
  }
}
