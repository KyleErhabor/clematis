query ActivityFeed($page: Int!, $perPage: Int!, $isFollowing: Boolean!, $hasRepliesOrTypeText: Boolean!) {
  Page(page: $page, perPage: $perPage) {
    activities(isFollowing: $isFollowing, hasRepliesOrTypeText: $hasRepliesOrTypeText, sort: [ID_DESC]) {
      ... on TextActivity {
        id
      }
      ... on ListActivity {
        id
      }
      ... on MessageActivity {
        id
      }
    }
  }
}
